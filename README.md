# RateUrCompany
RateUrCompany is a web-based feedback app with Playwright + TypeScript E2E automation using POM. It automates user registration, login, and rating flows, validates admin dashboard data, APIs, and SQLite DB, and generates HTML reports. Includes concurrent backend/frontend setup and CI-ready scripts.
